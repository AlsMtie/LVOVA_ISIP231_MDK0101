// Console.Write("Размер массива:");
// int N = int.Parse(Console.ReadLine());
// int [] m = new int[N];
// for (int i=0; i<N; i++){
//     m[i] = int.Parse(Console.ReadLine());
// }
// for (int i = N - 1; i >= 0; i--)
//     Console.Write(m[i] + " ");

// Console.WriteLine();

// double sum = 0;
// for (int i = 0; i < N; i++)
//     sum += m[i];
// double sr = sum / N;

// int nearest = m[0];
// for (int i = 1; i < N; i++)
//     if (Math.Abs(m[i] - sr) < Math.Abs(nearest - sr))
//         nearest = m[i];

// Console.WriteLine(nearest);
// Console.WriteLine(sr);